{
  "name": "@tam/react",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tam React runtime — context, renderers, OperationForm, ViewGrid. Source-only; consumed via path alias, tested independently here.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.2",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
